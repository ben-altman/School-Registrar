<h1>Courses offered in <%= @subject.name%>:</h1>

<ul>
<% @subject.courses.each do |c| %>
    <li><%= link_to "#{c.title}", course_path(c) %>, taught by <%= link_to "#{c.teacher.name}", teacher_path(c.teacher) %></li>
<% end %>

<%= link_to "Add a new #{@subject.name} course", new_subject_course_path(@subject) %>
</ul>