<h1><%= "Hello #{@teacher.name}" %></h1>

<%= flash[:notice] %>

<h2>Your Courses:</h2>
<ul>
<% @teacher.courses.each do |c| %>
    <li><%= link_to "#{c.title}", course_path(c) %>, subject - <%= link_to "#{c.subject.name}", subject_path(c.subject) %></li>
<% end %>
</ul>

<%= link_to "Add a new course", new_course_path %><br/>